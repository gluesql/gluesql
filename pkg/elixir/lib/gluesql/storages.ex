defmodule GlueSQL.Storages do
  @moduledoc """
  To properly create SQL database with `GlueSQL.glue_new()`, the user 
  should create storage instance first.  

  `GlueSQL.Storages` module has a list of usable storages.
  """
end
